# Copy to .env.local for dev. Set in hosting for prod.

VITE_SITE_URL=https://highpointlenders.com

# Indexable by default. Set to "false" only for staging/test builds that
# must stay out of search engines (emits noindex + Disallow: / robots.txt).
VITE_INDEXABLE=true
